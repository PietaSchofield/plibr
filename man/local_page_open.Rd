% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_local_page.R
\name{local_page_open}
\alias{local_page_open}
\title{Check and optionally open a local web page served from localhost}
\usage{
local_page_open(
  file_path,
  root_dir = "/var/www/html",
  host = "http://localhost",
  open = FALSE,
  request_timeout = 5
)
}
\arguments{
\item{file_path}{Character. Absolute path to a file within the server's document root.}

\item{root_dir}{Character. Absolute path to the server's document root.
Default "/var/www/html".}

\item{host}{Character. Host URL prefix. Default "http://localhost".}

\item{open}{Logical. If TRUE, open the URL (non-blocking) via \code{utils::browseURL}.
Default FALSE.}

\item{request_timeout}{Numeric. Seconds to wait for the HTTP request. Default 5.}
}
\value{
A list with elements:
\describe{
\item{url}{The constructed URL.}
\item{ok}{Logical, TRUE if status 200.}
\item{status}{Integer HTTP status code, or NA if unreachable.}
\item{opened}{Logical, TRUE if an attempt to open the URL was made (and \code{open=TRUE}).}
}
}
\description{
Given a file path under a web server's document root, this function constructs
the corresponding localhost URL, checks the HTTP status, and (optionally)
opens the URL in your default browser without blocking R.
}
\examples{
\dontrun{
res <- check_local_page_open(
  file_path = "/var/www/html/myproject/index.html",
  open = TRUE
)
if (res$ok) message("Page is up: ", res$url)
}

}
