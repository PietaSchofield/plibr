% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/knit_one.R
\name{knit_one}
\alias{knit_one}
\title{Knit a Single Bookdown Chapter to Standalone HTML}
\usage{
knit_one(
  chap_path,
  title = NULL,
  self_contained = TRUE,
  toc = TRUE,
  number_sections = TRUE,
  code_folding = "hide",
  output_file = NULL,
  root = getwd()
)
}
\arguments{
\item{chap_path}{Path to the R Markdown file of the chapter (e.g. \code{"chapters/03-methods.Rmd"}).}

\item{title}{Optional title for the standalone HTML. Defaults to \code{"Standalone: <basename>"}.}

\item{self_contained}{Logical. Should the HTML embed all resources (images, CSS, JS)?
Default is \code{TRUE}.}

\item{toc}{Logical. Include a table of contents? Default is \code{TRUE}.}

\item{number_sections}{Logical. Number the sections? Default is \code{TRUE}.}

\item{output_file}{Optional name for the rendered HTML output. If \code{NULL}, a default name
based on the chapter is used.}

\item{root}{Project root directory (used for relative paths). Defaults to the current working directory.}
}
\value{
Invisibly returns the path to the rendered HTML file.
}
\description{
Renders a single chapter or page from a \strong{bookdown/gitbook} project as a standalone HTML file,
preserving cross-references and using your existing project setup.
}
\details{
This function temporarily creates a wrapper \code{.Rmd} that sources shared setup code
(e.g. data paths, global chunk options) and includes the target chapter as a child document.

The function calls \code{\link[rmarkdown:render]{rmarkdown::render()}} on a temporary wrapper file using
\code{bookdown::html_document2} as the output format, so cross-references
(\verb{(\\#label)}) and citations are preserved.

You can customise shared data and chunk options by editing the file
\code{"R/common_setup.R"} in your project.
}
\examples{
\dontrun{
knit_one_chapter("chapters/03-methods.Rmd",
                 title = "Methods (standalone)",
                 output_file = "methods-standalone.html")
}

}
