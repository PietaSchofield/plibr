% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compile_rmd.R
\name{compile_rmd}
\alias{compile_rmd}
\title{Compile and Serve RMarkdown Documents with a Temporary Build Directory}
\usage{
compile_rmd(
  fileName,
  formats = c("bookdown::gitbook", "tufte::tufte_handout"),
  gitRepo = "uol",
  projName = "atlasps",
  codeDir = getwd(),
  output_dir = "/srv/http"
)
}
\arguments{
\item{formats}{Character vector. The output formats to render.
Supported formats include:
\itemize{
\item \code{"html_document"} (default HTML output)
\item \code{"powerpoint_presentation"} (PPTX output)
\item \code{"beamer_presentation"} (PDF via Beamer)
\item \code{"gitbook"} (multi-page HTML book)
\item \code{"tufte::tufte_handout"} (Tufte-style PDF)
}}

\item{gitRepo}{Character. The Git repository directory (\code{"uol"} or \code{"pers"}).}

\item{projName}{Character. The project name.}

\item{codeDir}{Character. The directory where the source \code{.Rmd} is located.}

\item{output_dir}{Character. The root directory where final outputs are stored. Defaults to \code{"/srv/http"}.}

\item{input_file}{Character. The name of the \code{.Rmd} file (without extension).}
}
\value{
Invisibly returns the file paths of the generated outputs.
}
\description{
This function compiles an RMarkdown (\code{.Rmd}) file into multiple output formats,
using a temporary build directory to avoid cluttering the main output directory.
}
\examples{
\dontrun{
# Compile only Distill and Tufte outputs
compile_rmd("my_presentation", formats = c("bookdown::gitbook", "tufte::tufte_handout"))
}
}
