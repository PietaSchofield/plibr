% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/docx_to_html.R
\name{docx_to_html}
\alias{docx_to_html}
\title{Convert a DOCX file to HTML}
\usage{
docx_to_html(
  docx_path,
  out_path = NULL,
  extract_media = TRUE,
  overwrite = FALSE,
  css_path = getOption("docx_to_html.css"),
  embed = TRUE,
  title = NULL
)
}
\arguments{
\item{docx_path}{Character. Path to the input \code{.docx} file.}

\item{out_path}{Character or \code{NULL}. Path to the output \code{.md}
file. If \code{NULL} (default), it is derived from \code{docx_path} by
replacing the \code{.docx} extension with \code{.md}.}

\item{extract_media}{Logical. If \code{TRUE} (default), embedded images
and other media are extracted into a folder named
\code{"<output_basename>_media"} alongside the output file.}

\item{overwrite}{Logical. If \code{FALSE} (default) and \code{out_path}
already exists, the function errors rather than silently overwriting.}

\item{css_path}{Character or NULL. Path to a CSS file to apply.}

\item{embed}{Logical. Embed CSS/media into a single portable HTML file.}

\item{title}{Character or NULL. Document title for the HTML \if{html}{\out{<title>}} tag.
If NULL (default), derived from the output filename (no extension).}
}
\description{
Convert a DOCX file to HTML
}
