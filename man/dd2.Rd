% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dd_2.R
\name{dd2}
\alias{dd2}
\title{dd2}
\usage{
dd2(
  dataset,
  number = NULL,
  table_type = "auto",
  limited = FALSE,
  buttons = FALSE,
  plen = NULL,
  caption = NULL,
  fixh = TRUE,
  fixc = list(leftColumns = 1),
  sigf = 3,
  dbug = FALSE
)
}
\arguments{
\item{dataset}{A data frame or tibble to display.}

\item{number}{Integer or NULL. If not NULL, shows only the first \code{number} rows.}

\item{table_type}{Character. One of \code{"auto"}, \code{"DT"}, \code{"kable"}, or \code{"flextable"}.
\code{"auto"} chooses DT for HTML and kable otherwise.}

\item{limited}{Logical. If TRUE, constrain HTML tables to a fixed-height container.}

\item{buttons}{Logical. If TRUE, include export buttons (HTML/DT only).}

\item{plen}{Integer. Rows per page in DT (HTML). If NULL, defaults to 10.}

\item{caption}{Optional character caption for the table.}

\item{fixh}{Logical. Fixed header for DT (HTML).}

\item{fixc}{List or NULL. Fixed columns options for DT (e.g., \code{list(leftColumns = 1)}).
Set \code{NULL} to disable.}

\item{sigf}{Integer. Significant figures for non-integer numeric columns in DT.}

\item{dbug}{Logical. If TRUE, sets internal defaults useful for manual testing.}
}
\value{
For HTML: an \code{htmlwidget} (DT datatable).
For PDF/LaTeX or non-HTML: a \code{knitr_kable} object.
For \code{table_type = "flextable"}: a \code{flextable} object.
}
\description{
Renders a data frame as an interactive DT table when knitting to HTML,
and as a LaTeX-friendly kable when knitting to PDF (or other non-HTML outputs).
Falls back to kable for non-HTML formats. Optionally limits rows, adds export
buttons in HTML, formats non-integer numerics to a fixed number of significant
figures, and supports fixed headers/columns in DT.

Adaptation of display_data
}
\details{
Display data as DT in HTML and kable in PDF (auto-switch)

Output type selection:
\itemize{
\item If \code{table_type = "auto"} (default), the function uses
\code{knitr::is_html_output()} to choose DT for HTML, otherwise kable.
\item For LaTeX/PDF, kable is produced with \code{booktabs = TRUE} and \code{longtable = TRUE}.
}

HTML behaviour (DT):
\itemize{
\item If \code{limited = TRUE}, a fixed-height scrolling area is used (\code{scrollY = "50vh"}).
\item \code{buttons = TRUE} adds "copy" and "csv" export buttons.
\item Fixed header uses the FixedHeader extension (\code{fixh = TRUE}).
\item Fixed columns use the FixedColumns extension via \code{fixc} (e.g., \code{list(leftColumns = 1)}).
\item All non-integer numeric columns are rounded with \code{DT::formatRound()} to \code{sigf} digits.
}

Interactive preview:
\itemize{
\item When running interactively \emph{outside} a knit (e.g., in the console),
the widget is saved to a temporary HTML file and opened in a browser.
}
}
\examples{
\dontrun{
# Auto-select: DT in HTML, kable in PDF
display_data(mtcars)

# Limit to 15 rows and sort/format in DT (HTML)
display_data(mtcars, number = 15, buttons = TRUE, sigf = 2)

# Force kable regardless of output format
display_data(mtcars, table_type = "kable")
}
}
