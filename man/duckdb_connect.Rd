% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/duckdb_connect.R
\name{duckdb_connect}
\alias{duckdb_connect}
\title{Connect to DuckDB with ICU pre-configured and resilient extension caching}
\usage{
duckdb_connect(
  dbfile = ":memory:",
  config = list(),
  extension_backup_dir = "~/duckdb_extension_backups",
  duckdb_platform = NULL,
  ...
)
}
\arguments{
\item{dbfile}{Path to db file, or ":memory:" (default).}

\item{config}{Extra duckdb() config, merged with sensible defaults.}

\item{extension_backup_dir}{Where your durable backup .gz files live.
Point this at somewhere that survives a wipe of ~/.duckdb — a dotfiles
repo, a synced drive, wherever you keep "never lose this" files.}

\item{duckdb_platform}{Override auto-detected platform string if needed.}

\item{...}{Passed through to dbConnect().}
}
\description{
Sets duckdb.home explicitly (so DuckDB never nags or silently switches to
a temp dir), disables network autoinstall, and — if the ICU extension
isn't already cached — restores it from a local backup .gz instead of
hitting the network.
}
