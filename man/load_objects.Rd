% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/save_objects.R
\name{load_objects}
\alias{load_objects}
\title{Load saved R objects from a directory}
\usage{
load_objects(names = NULL, dir = "artefacts", envir = parent.frame())
}
\arguments{
\item{names}{Optional character vector of object names (without \code{.rds} extension)
to load. If \code{NULL}, all \code{.rds} files in \code{dir} are loaded.}

\item{dir}{Directory containing the \code{.rds} files. Defaults to \code{"artefacts"}.}

\item{envir}{Environment into which objects should be loaded.
Defaults to the caller's environment.}
}
\value{
Invisibly returns a named list of loaded objects.
}
\description{
This helper reads \code{.rds} files previously saved with \code{\link[=save_objects]{save_objects()}}
and loads them into the specified environment.
}
\examples{
\dontrun{
  # load all saved artefacts
  load_objects(dir = "artefacts")

  # or just specific ones
  load_objects(c("tbl_summary", "model1"))
}
}
